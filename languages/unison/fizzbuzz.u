fizzbuzz : Nat -> Text
fizzbuzz n =
  use Nat mod
  if mod n 15 == 0 then "FizzBuzz"
  else if mod n 3 == 0 then "Fizz"
  else if mod n 5 == 0 then "Buzz"
  else Nat.toText n

main : '{IO, Exception} ()
main = do
  List.range 1 101 |> List.map fizzbuzz |> List.foreach printLine
