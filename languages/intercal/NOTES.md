INTERCAL's READ OUT statement emits Roman numerals, and its arithmetic is
built from the unary operators AND/OR/XOR over bit patterns rather than
ordinary integer ops. Producing the exact decimal text in `expected.txt`
requires a non-standard output routine.

Status: partial — not verified.
