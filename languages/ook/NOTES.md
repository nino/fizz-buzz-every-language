Ook! is a one-to-one transliteration of Brainfuck: each Brainfuck command
maps to a pair of Ook tokens. `generate.py` reads `../brainfuck/fizzbuzz.b`
and emits the equivalent Ook! program, so the two stay in sync.
